# 40 · 数据流总览

## 这一章讲什么

这一章把前 9 章的所有细节**串成 1 条线**——一个 session 从 Hermes 进来、到 evolve server 把它炼成 skill、再被下次同步拉回本地、最终被下一个 user 看到。这条线让你能"从头到尾看一次 SkillClaw"。

## 它在整个系统的哪个位置

细节层（整本书到这一章收尾，只有 40 和 41 两章；上面是模块层 15 章 10–32，再上面是总览层 3 章 00–02）。

## 设计目的

让"看过前面 9 章但还没在心里形成完整图"的读者读完本章后能"指着代码说：'哦，session 是这么从 Hermes 一直流到 skill 库更新的'"。

---

## 1. 时间线总览

**图例**：`→` 同子系统内步骤 / `⮕` 写入共享存储 / `↓` 跨子系统边界

```
[用户] ─► [agent] ─► [SkillClawAPIServer] ─► [upstream LLM]
                   │                         │
                   │ 1. POST /v1/chat/completions (X-Session-Id, X-Turn-Type)   [client]
                   │ 2. 注入 skills (XML)                                       [client]
                   │ 3. 转发 LLM ───────────────────────────────────────────►
                   │ 4. ◄── response + tool_calls                              [client]
                   │ 5. 提取 read/modified skills                              [client]
                   │ 6. 写 conversations.jsonl                                 [client]
                   │ 7. 触发 PRM task (async)                                  [client]
                   │
                   │ (turn 2 进来)
                   │ 8. fire 之前 turn 的 PRM 完成                             [client]
                   │ 9. close_session() → upload session.json                  [client]
                   │ 10. pull skills (skip modified)                           [client]
                   ↓
            [shared storage: {group_id}/]
                   │ pull_skills() 定时 reload (默认 30s,可在 `sharing.skill_reload_interval_seconds` 改,下限 5s)
                   ↓
              [evolve_server] (每 `evolve_interval` 秒,默认 600s,env `EVOLVE_INTERVAL`)
                  ──── client/server 边界 ────
                  [ES] 1. Drain sessions/
                  [ES] 2. Summarize (LLM #1: 每个 session)
                  [ES] 3. Judge (LLM #2: 每个 session, optional)
                  [ES] 4. Aggregate (按 _skills_referenced)
                  [ES] 5. Evolve (LLM #3: improve/create/skip, 每个 group)
                  [ES] 6. Verifier (LLM #4: optional, 默认关)
                  [ES] 7. Conflict check (sha256) → if conflict: Merge (LLM #5)
                  [ES] 8. Upload SKILL.md + version bundle + manifest
                  [ES] 9. Delete processed sessions
                  [ES] 10. Notify proxy reload (callback 模式)

> **注意**:客户端 10 步 (client 侧,标 1–10) 和 evolve_server 10 步 (server 侧,标 `[ES] 1–10`) **不一一对位**——字符集物理上已经分开（阿拉伯数字 vs 字母前缀），但即便如此，**逻辑上仍不要按编号对位理解**：客户端先 close_session 写入 `sessions/`,evolve server 之后下一次 cycle 才 drain 读取。同一份"step 5"在不同子系统里走的是不同事件。读时序时请按"客户端先 → server 后"的串行顺序理解,不要按编号对位。
```

## 2. 第一段：客户端代理（详见 12-14 章）

### 2.1 入口

```
[Hermes / OpenClaw / Claude Code / Codex / QwenPaw / ...]
  ↓ 改 base_url 到 http://127.0.0.1:30000/v1（17 章 claw_adapter 做的）
POST http://127.0.0.1:30000/v1/chat/completions
  Headers:
    X-Session-Id: <session_id>
    X-Turn-Type: main
    Authorization: Bearer <config.proxy_api_key>   # 若 config.proxy_api_key 非空则要求,空则不校验
  Body: {messages: [...], model: "skillclaw-model", tools: [...], ...}
```

> `X-Session-Id` 中时间戳的 `:` 换成 `-`（如 `hermes-2026-07-22T10-30-15`）是 Hermes 命名约定——避免 header value 解析问题;其他客户端可不遵循此格式,daemon 当不透明字符串。

`SkillClawAPIServer.chat_completions`：
1. `_mark_request_activity`（idle 探测器用）
2. `_check_auth`（`/internal/reload-skills` 强制 / chat-completions 不强制）
3. 读 body JSON
4. `_rewrite_new_session_bootstrap_prompt`（处理 OpenClaw 的 "A new chat session just started" 提示）
5. 解析 `session_id` / `turn_type` / `session_done`（header 优先 / body 兜底）
6. 调 `_handle_request(body, session_id, turn_type, session_done)`（详见 13 章）

### 2.2 `_handle_request` 内部

```text
# 1. 协议标记 (anthropic_messages 路径设置 _PROTOCOL_ANTHROPIC_MESSAGES)
# 2. touch session (更新 _session_last_active;给 idle sweeper 判断是否需要 close)
# 3. [OpenClaw] system prompt 压缩
# 4. [main turn] 注入 skills (_inject_skills 拼 <available_skills> 到 system 末尾)
# 5. context 截断 (_truncate_messages 按 max_context_tokens 贪心丢老 message)
# 6. 转发上游 LLM → 解析 tool_calls / read_skills / modified_skills
# 7. 写 turn_record → _session_turns / _pending_records
# 8. [_maybe_finalize_ready_turns] finalize 已 PRM 的 turn
# 9. [session_done] _close_session(session_id)
```

### 2.3 Turn 2 进来时：PRM 触发

```
POST /v1/chat/completions  (X-Session-Id: abc)
  body.messages = [user1, assistant1, user2, ...]
  ↓
_handle_request:
  - turn_type=main, turn_num=2
  - 注入 skills（2nd turn）
  - 转发 LLM → response2
  - _buffer_record → 触发 _fire_prm_scoring（因 next_state=turn2 的 user message 存在）
    - 创建 PRM task (prm_m=3 票)── 多数票(majority vote),不是中位/均值
    - task.add_done_callback(_on_prm_done)
  - turn_record = {turn_num: 2, prm_score: None, injected_skills: [...]}
  - _maybe_finalize_ready_turns：上一轮 PRM 还在跑 → 跳过
  - return
```

**PRM 单次评分的精确说法（与多数票规则）**:
- **单次评分 = 1 个分数 ∈ {+1, 0, -1}**(`prm_scorer._parse_prm_score` 只解析 `Score: 1` / `Score: -1` / `Score: 0`),**不是**"4 维度评分"(task_completion / response_quality / efficiency / tool_usage 是 Session Judge 的维度,不是 PRM 的);
- **`prm_m=3` 默认 3 票多数票**,平票或全 None → `0.0`(`_majority_vote` 在 `prm_scorer.py:99`),不是中位/均值;
- `_majority_vote` 返 `float`(投票值 `int` → `float(top[0])`),所以**最终 score ∈ {-1.0, 0.0, +1.0}**——这是 `record_feedback` 和 session.json 里 `prm_score` 字段的实际取值集合。
- `record_feedback(injected, score)` 的 `score` 就是这个 float 多数票值;§2.5 session.json 示例里若出现 `0.85` 之类小数,**不是 PRM 多数票结果**——那是别的来源(详见 [16 章](16-PRM与反馈.md) 关于 session judge / aggregator 的连续分),重写时勿混淆。

### 2.3.1 `_apply_prm_result` 同时回写到 injected + read

`_on_prm_done` → `_apply_prm_result(session_id, turn_num, prm_result)`(`api_server.py:2244+`)**对同一 turn 的 PRM 分数会调两次 `record_feedback`**:

```python
injected = turns[idx].get("injected_skills", [])
if injected and self.skill_manager:
    self.skill_manager.record_feedback(injected, score)
read = turns[idx].get("read_skills", [])
if read and self.skill_manager:
    read_names = [r["skill_name"] for r in read if isinstance(r, dict) and r.get("skill_name")]
    if read_names:
        self.skill_manager.record_feedback(read_names, score)
```

**这是有意行为**——"注入了"和"读了"是两类独立事件,被分别记分才能正确推 effectiveness。**同 skill 同时出现在两个 list 时**会被记两次(同 score),这是当前实现的语义;重写时可选择是否 dedup:dedup → 单一 score;不 dedup → 同 skill 收到两倍 feedback 但 score 一致。

### 2.4 Turn 3 / 4 / 5 / ... / N：PRM 累积

每个 turn 进来都触发上一轮的 PRM。`_prm_tasks[session_id][turn_num]` 跟踪。

### 2.5 `X-Session-Done: true` 或 idle 超时 → `_close_session`

标题完整配置项说明:**`session_idle_close_seconds`(默认 180s, ≤ 0 关闭 idle sweeper)**,idle sweeper 每 15s 扫一次,距上次活动 ≥ 180s 触发 close;或 `X-Session-Done: true` header 显式触发。

```text
_close_session(sid):
  1. _flush_pending_record(sid, None)            # conversations.jsonl 最后一行
  2. 触发 pending turn 的 PRM (finalize-skip 路径:只对尚未 finalize 的 turn 启动 PRM task,score 可能为 None)
  3. drain PRM (最多 `shutdown_drain_timeout_seconds` 秒,默认 15s)
  4. _finalize_turn_feedback → record_feedback + prm_scores.jsonl
  5. SkillManager._save_stats()                  # 强制刷盘
  6. _upload_session_data + _pull_skills_from_cloud
  7. 清 7 个 session_id 状态表
```

**session.json 内容**(注意 `prm_score` 是多数票 float,值域 `{-1.0, 0.0, +1.0}`):
```json
{
  "session_id": "abc",
  "timestamp": "2026-04-20T15:00:00Z",
  "user_alias": "alice",
  "num_turns": 5,
  "turns": [
    {"turn_num": 1, "user_turn_num": 1, "raw_turn_kind": "user", "prompt_text": "怎么 debug 这个 bug", "response_text": "...", "tool_calls": [...], "read_skills": [{"skill_name": "debug-systematically", "path": "..."}], "modified_skills": [], "tool_results": [...], "tool_errors": [], "injected_skills": ["debug-systematically", "react-patterns"], "prm_score": 1.0},
    ...
  ]
}
```

**`prm_score` 在 turn_record 里**——evolve server 后续 summarizer 拿到的 session 里能看到这个分（**作为信号**;PRM 失败 / drain 超时的 turn 该字段为 `null`,evolve server summarizer 看到 `null` 不报错但按"无信号"处理,详见 §10)。

## 3. 中段：共享存储（详见 20 章）

### 3.1 并行的三个时序：poller / record / evolve

三件事频率**各自独立**、并不"同时"——以下是它们的节奏:

- **client-side reload poller**（每 30s,jitter 10% + 失败 backoff 上限 60s,`sharing.skill_reload_interval_seconds` 配,下限 5s）：从 storage 拉 manifest → diff → pull 新 skill → `SkillManager.reload()`
- **client-side record**（每次 `_close_session`）：upload session.json 到 `sessions/<sid>.json`;无独立周期,与 close 事件同频
- **evolve server 周期**（每 600s,env `EVOLVE_INTERVAL` 配）：drain sessions → ...

### 3.2 共享存储长什么样

```
{group_id}/
├── manifest.jsonl          # evolve_server 在 _upload_skill 后**覆盖整文件**;不是行级 append
├── skills/<name>/
│   ├── SKILL.md            # evolve_server 写
│   └── files/...           # 多文件 bundle
├── skills/<name>/versions/v1/...vN/... # evolve_server 写;v 是固定字面前缀
├── sessions/<sid>.json     # 客户端 _close_session 时一次性写;每个 session 一份
├── validation_jobs/<job_id>.json       # evolve_server _queue_validation_job 时写;仅 publish_mode=validated 触发
├── validation_results/<job_id>/<user_alias>.json  # validation worker save_result 时写;按 <job_id>/<user_alias> 二级路径
├── validation_decisions/<job_id>.json  # evolve_server _finalize_validation_jobs 时写;仅 publish_mode=validated 触发
├── candidate_skills/<job_id>/SKILL.md  # evolve_server _queue_validation_job 时写;仅 publish_mode=validated 触发;Nacos 后端跳过
└── evolve_skill_registry.json          # evolve_server SkillIDRegistry.record_update 时写;Nacos 后端**不写**
```

**R1 修订**:`validation_results` 用 `/` 二级路径(`<job_id>/<user_alias>.json`),**不是** `__` 双下划线分隔;按 `user_alias` 区分不同 validator client。

**写入方 + 触发时机**——storage 重写者根据这条决策树决定 `put_object` 的调用点:

| 文件 | 写入方 | 触发时机 | 后端差异 |
|------|--------|----------|----------|
| `manifest.jsonl` | evolve_server `_upload_skill` | 每次成功 upload 后**覆盖整文件**(行级 JSONL 格式) | **Nacos 模式不写** |
| `sessions/<sid>.json` | 客户端 `_close_session` | 每次 close 时一次性写完整 turns | Nacos / OSS 都写(走 `nacos_skill_client` 或对象存储) |
| `validation_jobs/<job_id>.json` | evolve_server `_queue_validation_job` | **仅** `publish_mode="validated"` 时触发;异步、不阻塞主流程 | Nacos 模式不写(走 validation store) |
| `validation_results/<job_id>/<user_alias>.json` | validation worker `save_result` | 每个 validator client 跑完一次 replay/manual 后写 | Nacos 模式不写 |
| `validation_decisions/<job_id>.json` | evolve_server `_finalize_validation_jobs` | `min_results` 满足后立即聚合写;**仅** `publish_mode="validated"` | Nacos 模式不写 |
| `candidate_skills/<job_id>/SKILL.md` | evolve_server `_queue_validation_job` | 同 validation_jobs 同期写 | **Nacos 后端跳过** |
| `evolve_skill_registry.json` | evolve_server `SkillIDRegistry.record_update` | 每次 `_upload_skill` 成功后;**version 1-based int 自增** | **Nacos 后端不写**(Nacos 模式无持久化 registry,SHA 实时算) |

**`manifest.jsonl` 每行字段表**(`evolve_server/engines/workflow.py:313-327` 写入):
```json
{
  "name": "<skill name>",
  "skill_id": "<12-hex SHA256>",
  "version": <int, 1-based>,
  "sha256": "<content sha256>",
  "tree_sha256": "<bundle tree sha256>",
  "format": "bundle_v1",
  "entrypoint": "SKILL.md",
  "files": [{"path": "...", "sha256": "...", "size": N}, ...],
  "uploaded_by": "evolve_server",
  "uploaded_at": "2026-04-20T15:00:00+00:00",
  "description": "...",
  "category": "coding"
}
```

**`evolve_skill_registry.json` entry schema**(`evolve_server/core/skill_registry.py`):
```json
{
  "<name>": {
    "skill_id": "<12-hex sha256(name)[:12]>",
    "version": <int, 1-based, record_update 自增>,
    "content_sha": "<sha256>",
    "history": [
      {"version": <N>, "content_sha": "...", "timestamp": "...", "action": "improve|create|merge|optimize_description"},
      ...
    ]
  }
}
```

`save_manifest`(`oss_helpers.py:99-103`)用 `"\n".join(records) + "\n"` 写入——是**覆盖整文件 + 行级 JSONL 格式**(`save_manifest` 全量读后全量写;并发 push 会丢一方的变更,无 ETag 保护——见 §10)。

**`validation_jobs/<job_id>.json` 字段表**(`evolve_server/engines/workflow.py:538-556` 写入,共 13 个字段):
```json
{
  "job_id": "YYYYMMDDHHMMSS-<slug>-<8hex>",
  "status": "pending_validation",
  "created_at": "2026-04-20T15:00:00+00:00",
  "candidate_skill_name": "...",
  "candidate_skill_id": "...",
  "candidate_skill": {<dict>},
  "current_skill": {<dict or null>},
  "proposed_action": "improve_skill|create_skill|optimize_description",
  "source": "skill_group|no_skill",
  "rationale": "...",
  "session_ids": ["..."],
  "session_evidence": [...],
  "replay_cases": [
    {"session_id": "...", "turn_num": 1, "instruction": "...", "baseline_response": "...", "candidate_response": "..."}
  ],
  "min_results": <int, default 1>,
  "min_approvals": <int, default 1>,
  "min_score": <float, default 0.75>,
  "max_rejections": <int, default 1>
}
```

**`validation_results/<job_id>/<user_alias>.json` 字段表**(`skillclaw/validation_worker.py:save_result`):
```json
{
  "job_id": "...",
  "user_alias": "alice",
  "created_at": "...",
  "validator_mode": "replay" | "manual",
  "decision": "accept" | "reject",
  "accepted": true | false,
  "score": <float>,
  "threshold": <float>,
  "reason": "...",
  "checks": {"grounded_in_evidence": <f>, "preserves_existing_value": <f>, ...},
  "replay_summary": {"case_count": N, "baseline_mean_score": <f>, "candidate_mean_score": <f>, "cases": [...]}
}
```

**`validation_decisions/<job_id>.json` 字段表**(`save_decision`):
```json
{
  "job_id": "...",
  "status": "published_after_validation" | "validation_rejected" | "skipped",
  "published_action": "...",
  "result_count": N,
  "accepted_count": N,
  "rejected_count": N,
  "mean_score": <float>,
  "decided_at": "..."
}
```

## 4. 第三段：Evolve Server（详见 30-32 章）

### 4.1 `EvolveServer.run_once`（workflow 引擎，6 步流水线）

```text
# Stage 1: Drain       → list_session_keys + read_json_object
# Stage 2: Summarize   → LLM #1 (每个 session 并行)  + _trajectory / _skills_referenced 程序元数据
# Stage 3: Session Judge → LLM #2 (optional, 默认启用)  → _judge_scores
# Stage 4: Aggregate   → 按 skill name 聚合 sessions, 落不到任何 skill 的进 __no_skill__
# Stage 5: Evolve + Create → LLM #3 每个 group: improve / optimize_desc / skip (no_skill 走 create)
# Stage 6: Upload      → verifier (LLM #4 optional) → conflict detect → merge (LLM #5 optional) → _upload_skill
```

### 4.2 Evolve LLM 看到的 prompt（`evolve_skill_from_sessions`）

**完整 prompt 模板结构** = system (固定字符串) + user (三段: `## Current skill` / `## Session evidence (N sessions)` / `## Existing skill names in the library`;N 由 sessions 数决定)。LLM 自由发挥的部分是 `## Session evidence` 内每个 session 的 `**Analysis**` 摘要;`## Current skill` / `## Existing skill names in the library` / system 都是**程序拼好**给 LLM 看的。

```python
system = "You are a skill editor for SkillClaw. Given the current skill '{skill_name}' and N sessions that reference it, decide: improve / optimize_description / skip. Return JSON {action, skill, rationale}."
user = (
    "## Current skill\n"
    "Name: debug-systematically\n"
    "Description: Use when diagnosing a bug...\n"
    "Category: coding\n"
    "Content:\n```\n# Debug Systematically\n1. Reproduce...\n```\n\n"
    "## Session evidence (3 sessions)\n"
    "### Session abc {avg_prm=0.85, has tool errors, skills: [debug-systematically]}\n"
    "**Trajectory**:\n    user(...)\n    assistant(...)\n    read_skill(path=...)\n"
    "**Analysis**:\nThe agent followed the skill but missed step 3 (verify root cause)...\n\n"
    "## Existing skill names in the library\n"
    "debug-systematically, react-patterns, ..."
)
```

> "skill editor" 是 SkillClaw 给 Evolve 阶段 LLM 起的**角色名**(区别于客户端的 user/agent 角色)——INDEX [02 章](02-核心概念词典.md) 已定义。

LLM 返回(`DecisionAction` **字面值**是字符串,不是属性名;详见 `evolve_server/core/constants.py:35-41`):
```json
{
  "action": "improve_skill",
  "skill": {
    "name": "debug-systematically",
    "description": "Use when diagnosing a bug. Gather evidence before forming hypotheses. **Now includes explicit verification step.** NOT for: simple typo fixes.",
    "category": "coding",
    "content": "# Debug Systematically\n\n1. Reproduce.\n2. **Verify root cause before fixing** (NEW based on session evidence).\n3. Apply minimal fix.\n4. Re-run.\n"
  },
  "rationale": "Session abc missed verification step; adding it explicitly. Other steps stay the same to preserve behavior."
}
```

**`skill` 字段表——LLM 必填 4 字段 + server 追加 6 字段**:

| 字段 | 谁填 | 类型 | 格式 / 计算方式 |
|------|------|------|------------------|
| `name` | LLM 必填 | str | kebab-case `^[a-z][a-z0-9-]{1,63}$`;CREATE 路径走 `_sanitise_name`,IMPROVE 路径被 `_materialize_skill` 强制 overwrite 为 `current_skill['name']`(LLM 想 rename 也会被覆盖) |
| `description` | LLM 必填 | str | 单段文本;OPTIMIZE_DESC 只改它,body 继承老的 |
| `category` | LLM 必填 | str | 自由文本;manifest 写时若 LLM 没给 → 默认 `"general"` |
| `content` | LLM 必填 | str | markdown body;`build_skill_md(skill)` 拼成完整 SKILL.md |
| `version` | server 追加 | int | 1-based,`SkillIDRegistry.record_update` 自增;LLM 不算、不写 |
| `sha256` | server 追加 | str | `sha256(content.encode("utf-8")).hexdigest()`;LLM 算不出 |
| `tree_sha256` | server 追加 | str | `sha256(按 (path, sha, size) 排序拼,中间 \0 + 末尾 \n)`,见 `bundle_tree_sha256`(`skillclaw/skill_bundle.py:103`) |
| `files` | server 追加 | list | `bundle_file_records` 扫 bundle 目录生成;每文件 `{path, sha256, size}`;LLM 不返回 files,server 在 `save_version_bundle` 里由 bundle 目录直接打包 |
| `format` | server 追加 | str | 固定字面值 `"bundle_v1"`;LLM 不写 |
| `skill_id` | server 追加 | str | `sha256(name.encode()).hexdigest()[:12]`(12-hex);`SkillIDRegistry.get_or_create` 派生 |

> LLM 必填字段集只有 4 个(`name` / `description` / `category` / `content`);其余 6 字段(`version` / `sha256` / `tree_sha256` / `files` / `format` / `skill_id`)由 evolve server 在 LLM 返回后**追加**——LLM 不算、不写。重写 `_parse_evolve_result` / `_resolve_and_upload` 时,LLM 不会返回后 6 字段,server 必须在 `_upload_skill` 之前补齐。

**`DecisionAction` 4 个值**(`core/constants.py:35-41`):
- `CREATE = "create_skill"`(从 no-skill bucket 造新 skill)
- `IMPROVE = "improve_skill"`(老 skill 改写)
- `OPTIMIZE_DESC = "optimize_description"`(只改 description,body 继承)
- `SKIP = "skip"`(LLM 决定不动)

**4 决策的建议准则(prompt 实际不强制、LLM 自由发挥,以下为参考骨架,prompt 里通常不显式列)**:
- `sessions < 3` → SKIP(数据不够,不值得演进)
- `所有 session PRM < 0.3`(注意:PRM 值域是 {-1.0, 0.0, +1.0},0.3 实际是 `avg_prm <= 0.0` 即负信号占多数)→ OPTIMIZE_DESC(description 问题大,body 还能用)
- 目标 skill 已存在且 `session evidence` 显示 description 能改进 → IMPROVE
- 目标 skill 不存在(no-skill bucket) → CREATE
- 4 决策以外的字符串 / `action` 字段缺失 → **默认 SKIP**(`execution.py:447`)

> **默认是 SKIP**:`_parse_evolve_result`(`execution.py:447`)的 action 默认值是 `DecisionAction.SKIP`——LLM 不返回 action 字段时,默认**跳过**该 skill。读者按"IMPROVE 是默认"实现,会跟代码不一致。
>
> **CREATE 路径的兜底降级**:`_parse_evolve_result`(`execution.py:456-465`)——LLM 偶发返回 `create_skill` 但 `name == skill_name`(等于改名没改)时,会被**静默改写**为 `IMPROVE`,避免同名 create 改写历史。
>
> **`_materialize_skill` 强制 name 行为**:`workflow.py:760-764` 在 `_materialize_skill` 里:**`action_type == DecisionAction.IMPROVE` 时强制 overwrite skill name 为 `current_skill['name']`**——LLM 想 rename 也会被覆盖;`CREATE` 路径才走 `_sanitise_name`。

### 4.3 Upload

**本节以 OSS 路径为主线**;`skill_storage_backend=nacos` 时跳过 `manifest.jsonl` / `evolve_skill_registry.json` 写入,直接调 `nacos_skill_client.upload_skill_zip`(version 由 Nacos 服务端管,SHA 实时算);`skill_storage_backend=local` 仅本地文件、不写 manifest。

```python
# 1. _sanitise_name: "debug-systematically" → 同样
# 2. _run_skill_verifier: enabled=False 跳过
# 3. publish_mode="direct" 跳过 _queue_validation_job
# 4. _resolve_and_upload(skill, "improve_skill")
#    # _resolve_and_upload(skill: dict, action_type: str)
#    #   action_type ∈ {improve_skill, create_skill, optimize_description, skip, merge}
#    - _detect_conflict(workflow.py:338-352): sha256 不一样 → True
#      * OSS / S3 / Local 路径:用 id_registry.get_content_sha(name)
#      * Nacos 路径:用 _fetch_skill 拉远端 SKILL.md 重算 SHA(没有 registry 持久化)
#    - _fetch_skill("debug-systematically") → 老 SKILL.md
#    - execute_merge(llm, existing, incoming) → LLM 合并 → merged_skill
#    - _upload_skill(merged_skill, "merge")
#      - object_key = "default/skills/debug-systematically/SKILL.md"
#      - put_object(...)
#      # record_update(name, content_sha, action, bundle_record)
#      #   4 参均必填;action ∈ {create, improve, optimize_description, merge}
#      - SkillIDRegistry.record_update(name, sha, "merge", bundle_record)
#      - save_version_bundle(v8/...)   # v 是固定字面前缀,version=8 是 1-based int
#      - manifest[name] = {...version=8...}   # version 是 int,路径里的 v8 与字段值无关
#      - save_manifest(...)            # 覆盖整文件;非原子;并发 push 会丢变更
# 5. return action="merge", uploaded=True

# 6. registry save_to_oss
# 7. delete_session_keys(N sessions,按实际数量)
# 8. _append_history(summary)
# 9. uploaded_skills > 0 → _notify_proxy_reload
#    - url = config.evolve_proxy_reload_url (默认 http://127.0.0.1:30000/internal/reload-skills)
#    - Authorization: Bearer <config.proxy_reload_api_key>  # 与客户端 proxy_api_key 不同
#    - 3 次重试 POST(workflow.py:991-1012)
#      * attempt 0/1/2,间隔 1s/2s(线性 backoff 1.0 * (attempt + 1))
#      * 单次 timeout=5s
#    - 客户端对应:_trigger_evolve(api_server.py:3121-3141)同样 3 次重试,1s/2s,但 timeout=300s
```

### 4.4 替代：Agent Engine

**引擎选型**——通过 `evolve.engine = "workflow" | "agent"` 配置项切换(env `EVOLVE_ENGINE` 或 CLI `--engine`,默认 `"workflow"`);**没有**"两种都跑"的中间态,evolve server 启动时一次性选定。

- **workflow 引擎**(默认)适合:大批量 session 批处理 / 成本敏感 / 不需要 OpenClaw 进程级隔离的场景
- **agent 引擎**适合:需要 OpenClaw 进程级隔离 / 多步调试 / 跨轮记忆(`--no-fresh`)/ 让 LLM 自由写多个 skill 的场景

`AgentEvolveServer.run_once`:
1. Drain N sessions(按 `list_session_keys` 实际数量)
2. Summarize(用同一个 summarizer pipeline)
3. Workspace reset (`--fresh`) 或保留
4. Fetch all skills bundles → 写 workspace/skills/
5. 写 sessions/ + manifest.json + EVOLVE_AGENTS.md + 4 个 bootstrap
6. Snapshot tree_sha
7. 启 OpenClaw 子进程(session_id 跨轮记忆)
8. OpenClaw 读 sessions/、读 EVOLVE_AGENTS.md、写 skills/<name>/SKILL.md
9. Snapshot after
10. collect_changes: diff → 2 changes (create + improve)
11. 上传 N 个 skill
12. Cleanup

**关键差异**:workflow 引擎是**"evolve server 调 LLM"**,agent 引擎是**"evolve server 启 OpenClaw 进程,OpenClaw 调 LLM"**。

## 5. 第四段：Client Reload

### 5.1 Callback 模式

evolve server 的 `_notify_proxy_reload` POST `/internal/reload-skills`:
```python
# 3 次重试（间隔 1s / 2s）
# url = config.evolve_proxy_reload_url (默认 http://127.0.0.1:30000/internal/reload-skills)
# Authorization: Bearer <config.proxy_reload_api_key>  # 与客户端 proxy_api_key 不同;这把是 evolve server → 客户端 reload 端点专用
resp = await client.post(url, headers={"Authorization": f"Bearer {api_key}"})
```

`SkillClawAPIServer.reload_skills`:
```python
await self._check_auth(authorization)
await self._pull_skills_from_cloud()
skill_count = len(self.skill_manager.get_all_skills())
return {"ok": True, "skills": skill_count}
```

`_pull_skills_from_cloud(skip_names=...)`:
```python
hub = SkillHub.from_config(self.config)
pull_result = hub.pull_skills(self.config.skills_dir, skip_names=skip_names)
# downloaded=N, skipped=M, failed=K, deleted=D
if self.skill_manager and (downloaded > 0 or deleted > 0 or restored_from_backup):
    self.skill_manager.reload()
```

`SkillHub.pull_skills` mirror=True(默认)→ 把 manifest 镜像到本地 + 删 extras + 失败回滚。

### 5.2 Poll 模式（默认）

`_skill_reload_poll_loop` 每 `sharing.skill_reload_interval_seconds`(默认 30s,下限 5s)跑一次(带 10% jitter + 失败 backoff 上限 60s):

```python
while True:
    if not first_pull: await asyncio.sleep(interval + jitter + backoff)
    try:
        await self._pull_skills_from_cloud()
        consecutive_failures = 0
    except: consecutive_failures += 1
```

**30s 默认延迟** = 新 skill 拉取最多滞后 30s(默认配置下);pull 成功后,**`skill_manager.refresh_if_changed()` 在下一个 main turn 入口**被 `_inject_skills` 调到,比对 `self._skill_fingerprint` 与当前,变化即重建 prompt(详见 §6.1)。

## 6. 第五段：用户看到更新

### 6.1 下一次 main turn

```
[用户]  "我又要 debug 一个 bug"
  ↓
[Hermes] POST /v1/chat/completions
  ↓
[SkillClawAPIServer]
  - skill_manager.refresh_if_changed()   # 由 §5.2 poll loop 在 pull 成功后设 _skill_fingerprint;此处比对变化
  - skill_manager.build_injection_prompt(max_chars=30_000)   # max_chars=30_000 在 Chat Completions 路径硬编码(非 config)
  - <available_skills> 里 "debug-systematically" 的 description 已经是 "Now includes explicit verification step"
  - record_injection(["debug-systematically", ...])
  - 转发 LLM
  - LLM 看到新 description → 决定 read SKILL.md
  - SKILL.md 的 content 也已经更新（多了"Verify root cause before fixing"）
  - 走完
```

**端到端延迟**:
- session_done → session upload:~立即
- evolve server 周期:~600s(`EVOLVE_INTERVAL` 默认)
- evolve server 处理:~5-30s(5-10 个 LLM 调用)
- 拉回本地:30s(poll)或 <1s(callback)
- **总延迟**:5-15 分钟(默认配置)

> 数字基于 DeepSeek / Qwen3 中等 session (5-15 turn) 经验值,**非 SLA**——不同 LLM / session 规模差异大。

## 7. 数据流关键时序图

> 本节时序图为**示意**,时长均为"由用户行为 / 配置项决定",非系统固定时序。

```
T+0s:  user turn 1 (POST /v1/chat/completions)
       → record 1 buffered
       → PRM task 1 created (waiting for turn 2)
T+Xs:  user turn 2 (由用户决定,几秒~几小时都有可能)
       → record 2 buffered
       → PRM task 1 done (score ∈ {-1.0, 0.0, +1.0} 多数票)
       → record 1 finalized: prm_score=1.0, SkillManager.record_feedback
T+Ys:  user turn 5 + X-Session-Done: true (由用户决定)
       → _close_session (session_idle_close_seconds 配或显式 done)
       → upload session.json
       → pull skills (manifest unchanged)
T+`evolve_interval`(默认 600s): evolve server 下一次 cycle
       → drain 1 session (the one we just uploaded)
       → summarize (LLM ~3s)
       → aggregate
       → evolve "debug-systematically" (LLM ~5s, "improve_skill")
       → upload (sha256 changed, conflict path; merge with LLM ~5s; upload merged)
       → manifest["debug-systematically"].version = 8
       → notify proxy reload
T+`evolve_interval`+~5s: SkillClawAPIServer reload
       → pull_skills("debug-systematically" sha mismatch → download new SKILL.md)
       → skill_manager.reload()
       → _skill_fingerprint 变化
T+`evolve_interval`+~6s (or next main turn): user turn 6
       → skill_manager.refresh_if_changed() detects change
       → <available_skills> now has updated description
       → LLM reads new SKILL.md
```

## 8. 关键对象 ID 与命名约定

| 对象 | ID / 命名 |
|---|---|
| `session_id` | 来自 client header(OpenClaw 产生)/ `tui-<model>-<8hex>`(启发式) |
| `skill_name` | `^[a-z][a-z0-9-]{1,63}$`(kebab-case) |
| `skill_id` | `sha256(skill_name)[:12]`(与 `SkillManager._parse_skill_md` 一致;12-hex) |
| `group_id` | `"default"` 或用户配置(共享命名空间) |
| `user_alias` | `$USER` 或 `sharing.user_alias` |
| `version` | 1-based int(`SkillIDRegistry.record_update` 自增) |
| `tree_sha256` | `sha256(按 (path, sha, size) 排序拼,\0 + \n 分隔)`(见 `bundle_tree_sha256`) |
| `job_id` | `YYYYMMDDHHMMSS-<slug>-<8hex>` |
| `bundle` 路径 | `skills/<name>/SKILL.md`(entrypoint) + `files/<rel_path>` |
| `version` 路径 | `skills/<name>/versions/v<N>/...`;**`v` 是固定字面前缀**,与 `version` 字段值(int)无关 |

## 9. 待核 / 已知限制

> 本章含 **5 条 [待核]**,集中于 §10;另外有少量 inline 注释的"待核"细节(见 §3.2 manifest 写覆盖 / §2.5 PRM 失败 `prm_score=null` 等)。按 agent.md 规则拆为两类:`[待核]` = 需 openbook 对照或部署环境验证才能定论,`[TODO]` = writer 自己未决定。本节所有"待确认"全部标 `[待核]`——无 `[TODO]`。

> **关于 `file:line` 引用**:本章早期版本大量 `api_server.py:<line>` 引用与实际行号错位 100~2200 行(如 `_handle_request` 文档说 `api_server.py:75-` 实际在 `:2311`,`_resolve_tui_session` 文档说 `:70-` 实际在 `:1861`)。**所有行号引用会随代码变更漂移**——以**函数名 + git blame** 为准,行号仅作 debug 时的"先到这里看看"提示。

## 10. PRM 反馈回路(端到端视角)

5 条遗留决策以**两列对比**呈现——左列是 openbook 已验证的"当前实现行为"(写死,重写者按这个对齐);右列是"重写者可决策 / 部署方决定"(留给重写 / 部署环节按需调整)。主流程不再标 [待核]。

| # | 已实现的当前行为(对齐) | 重写者可决策 / 部署方决定 |
|---|------------------------|----------------------------|
| 1 | **session.json 没有去重**:同 session_id 二次 upload(`_close_session` 一次 + `dashboard.export_local_sessions` 一次)会**完全覆盖**——内容相同所以无害。 | 是否在 dashboard export 时加 `if existing == content: skip` 去重保护。 |
| 2 | **Evolve server 不读 conversations.jsonl**:它只读共享存储的 `sessions/<sid>.json`。PRM 失败 / turn 还没 finalize 的 session 不会被 evolve(`_close_session` 之前 PRM 强制 drain `shutdown_drain_timeout_seconds`,默认 15s)。 | drain 15s 超时后,PRM 失败的 turn 上传时 `prm_score=null`——是否给这类 turn 显式 `_prm_failed: true` 标志位。 |
| 3 | **Manifest 写覆盖不原子**:`save_manifest` 全量读后全量写——并发 push 会丢一方的变更(无 ETag 保护)。 | 是否加 ETag 保护 / 是否改用 append-only 写入 / 是否加写入锁。 |
| 4 | **`prm_score` 在 `turn_record` 里但 upload 后是 `null`(如果 PRM fail)**:evolve server 的 summarizer 不会额外说明——按"无信号"处理,不报负信号。 | 是否在 session.json 顶层加 `_prm_failed_count` 聚合字段,让 summarizer 看到"本 session 整体弱信号"。 |
| 5 | **Evolve server 周期内 concurrency = 1**:多个 cycle 不能并行(用 `self._running` 标志位)——长 cycle 会阻塞下一次。 | 是否加 cycle 队列 / 是否允许多 cycle 并行(配 cycle_id 防冲突) / 是否对长 cycle 加超时报警。 |

---

→ **下一篇**:[41-部署形态](41-部署形态.md) — 单机自用 / 团队共享 / validated 三种部署模式
